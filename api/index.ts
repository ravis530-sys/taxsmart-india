// Vercel serverless entry point.
// Vercel's @vercel/node builder compiles this TypeScript file and bundles
// all transitive imports — no separate compile step needed.
import app from '../server/src/index';
export default app;
