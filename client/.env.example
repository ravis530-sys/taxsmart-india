# Leave empty — frontend and backend are deployed on the same Vercel domain,
# so all /api/* calls are same-origin (no separate URL needed).
# Only set this if you deploy the backend somewhere else.
VITE_API_URL=
